import "package:flutter/material.dart";

class paymantpage extends StatelessWidget {
  const paymantpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

